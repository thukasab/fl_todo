仮想環境のアクティベート
env\Scripts\activate

git 初期化
git init

git 追加
git add .

git コミット
git commit -m "first commit"

状態の確認
git status 

git の削除
del .git

ユーザーネームの確認
git config --global user.name

ユーザーネームの登録
git config --global user.name "****"

ブランチの切り替え(新規作成も兼ねる)
git checkout -b *****(例：add_mod1)

ブランチの確認
git branch

master　の内容をローカルリポジトリに反映させる
git pull origin master


heroku login
haya---i@0---

heroku create アプリ名

git push heroku master


